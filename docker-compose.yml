services:
  control-plane:
    build: .
    container_name: olympus-control-plane
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      # Add these later when the real integrations are wired:
      # PROXMOX_URL=
      # PROMETHEUS_URL=
      # LOKI_URL=
      # TAILSCALE_API_URL=
      # CAMERA_URL=
