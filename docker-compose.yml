# Optional local development container; production runs on Hermes/K3s.
services:
  olympus:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
    env_file:
      - .env.local
